# Dialogue And Drama

- Current BQ patches `DramaManager.ParseLine(Dictionary<string,string>)`, `DramaEventTalk.InitDialog()`, and `DialogDrama.SetText(string,bool)` (`VERIFIED-METADATA`).
- The 2026-08-29 runtime log says `Drama choice projector installed` (`VERIFIED-RUNTIME`).
- The generic talk guard checks `DramaManager.setup.book == "_chara"` and `step == "main"` (`VERIFIED-METADATA`; generic semantics are `SOURCE-OBSERVED` from previous installed implementation reading).
- `DramaEventTalk.choices`, `funcText`, `text`, and `AddChoice(DramaChoice)` exist (`VERIFIED-METADATA`).
- `EVENT.DramaParseAction` exists as a constant, but `DramaManager.ParseLine(Dictionary<string,string>)` handles custom drama actions through `CustomDramaExpansion.ParseAction(...)`; no publish point for `EVENT.DramaParseAction` was found in the installed ParseLine body (`VERIFIED-METADATA`, `SOURCE-OBSERVED`). It should not replace the current Harmony lifecycle patches.
- `DramaEventTalk.AddChoice(DramaChoice)` appends to `choices`, and `DramaEventTalk.Play()` iterates the list and assigns numeric shortcuts by position; no static hard choice-count limit was found (`SOURCE-OBSERVED`). Scrolling/clipping with BQ's fixed entries plus up to seven action choices remains a visual runtime question.
- Raw NPC speech exists on `Card`, inherited by `Chara`: `SayRaw(string,string,string)` and `TalkRaw(string,string,string,bool)` with optional trailing parameters (`VERIFIED-METADATA`). `SayRaw` requires a synced card and non-empty text, then calls `CardRenderer.Say(text, color, 0)` (`SOURCE-OBSERVED`). `TalkRaw` can also log to `Msg` after showing the balloon (`SOURCE-OBSERVED`). BQ's `ElinBark` resolver should bind this actor route before falling back to `Msg.SayRaw`; runtime still needs to verify visibility during open Drama.
