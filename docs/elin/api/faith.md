# Faith

- `Chara.idFaith` and `Chara.elements.Value(85)` are current BQ reads (`VERIFIED-METADATA`).
- Runtime PC faith read `harvest` and piety `54` (`VERIFIED-RUNTIME`).
- `SourceReligion.csv` row id `harvest` is `Kumiromi of Harvest`, with aliases `Harvest,Kumiromi` and domain `domHarvest` (`SOURCE-DATA`).
- Other installed religion ids include `eyth`, `wind`, `earth`, `element`, `machine`, `luck`, `healing`, `oblivion`, `harmony`, `trickery`, `moonshadow`, and `strife` (`SOURCE-DATA`).
- BQ should compare faith routes against SourceReligion ids such as `harvest`, not display names such as `Kumiromi`, unless a mapping is explicitly documented (`SOURCE-DATA`, `VERIFIED-RUNTIME`).
